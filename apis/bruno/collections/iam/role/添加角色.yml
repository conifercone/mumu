info:
  name: 添加角色
  type: http
  seq: 1

http:
  method: POST
  url: "{{iam-service-url}}/role/add"
  body:
    type: json
    data: |-
      {
        "code": "test2",
        "name": "测试",
        "permissionIds": [
          1,
          2
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
