info:
  name: 根据id查询角色
  type: http
  seq: 15

http:
  method: GET
  url: "{{iam-service-url}}/role/findById/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
