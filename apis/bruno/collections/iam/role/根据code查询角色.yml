info:
  name: 根据code查询角色
  type: http
  seq: 16

http:
  method: GET
  url: "{{iam-service-url}}/role/findByCode/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
