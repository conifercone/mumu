info:
  name: 根据code删除角色
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{iam-service-url}}/role/deleteByCode/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
