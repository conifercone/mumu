info:
  name: 添加后代角色
  type: http
  seq: 11

http:
  method: PUT
  url: "{{iam-service-url}}/role/addDescendant"
  body:
    type: json
    data: |-
      {
        "descendantId": 4,
        "ancestorId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
