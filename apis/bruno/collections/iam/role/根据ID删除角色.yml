info:
  name: 根据ID删除角色
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{iam-service-url}}/role/deleteById/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
