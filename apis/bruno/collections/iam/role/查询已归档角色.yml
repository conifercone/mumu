info:
  name: 查询已归档角色
  type: http
  seq: 4

http:
  method: GET
  url: "{{iam-service-url}}/role/findArchivedAll?current=1&pageSize=10"
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
