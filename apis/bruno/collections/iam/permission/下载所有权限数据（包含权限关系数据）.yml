info:
  name: 下载所有权限数据（包含权限关系数据）
  type: http
  seq: 11

http:
  method: GET
  url: "{{iam-service-url}}/permission/downloadAllIncludePath"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
