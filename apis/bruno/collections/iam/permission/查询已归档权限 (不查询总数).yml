info:
  name: 查询已归档权限 (不查询总数)
  type: http
  seq: 18

http:
  method: GET
  url: "{{iam-service-url}}/permission/findArchivedAllSlice?current=1&pageSize=10"
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
