info:
  name: 根据code删除权限
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{iam-service-url}}/permission/deleteByCode/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
