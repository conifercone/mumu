info:
  name: 查询权限
  type: http
  seq: 9

http:
  method: GET
  url: "{{iam-service-url}}/permission/findAll?current=1&pageSize=10&name=数据"
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
    - name: name
      value: 数据
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
