info:
  name: 根据id删除权限
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{iam-service-url}}/permission/deleteById/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
