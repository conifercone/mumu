info:
  name: 根据id从归档恢复权限
  type: http
  seq: 8

http:
  method: PUT
  url: "{{iam-service-url}}/permission/recoverFromArchiveById/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
