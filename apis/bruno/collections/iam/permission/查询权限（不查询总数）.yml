info:
  name: 查询权限（不查询总数）
  type: http
  seq: 12

http:
  method: GET
  url: "{{iam-service-url}}/permission/findAllSlice?current=1&pageSize=10&name=数据"
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
    - name: name
      value: 数据
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
