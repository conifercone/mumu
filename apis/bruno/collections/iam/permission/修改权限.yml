info:
  name: 修改权限
  type: http
  seq: 4

http:
  method: PUT
  url: "{{iam-service-url}}/permission/updateById"
  body:
    type: json
    data: |-
      {
        "id": "1",
        "name": "test_updated"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
