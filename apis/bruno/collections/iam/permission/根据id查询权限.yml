info:
  name: 根据id查询权限
  type: http
  seq: 13

http:
  method: GET
  url: "{{iam-service-url}}/permission/findById/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
