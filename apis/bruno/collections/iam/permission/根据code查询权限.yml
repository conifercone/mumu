info:
  name: 根据code查询权限
  type: http
  seq: 14

http:
  method: GET
  url: "{{iam-service-url}}/permission/findByCode/admin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
