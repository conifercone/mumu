info:
  name: 当前账号添加系统设置
  type: http
  seq: 7
  tags:
    - account

http:
  method: POST
  url: "{{iam-service-url}}/account/addSystemSettings"
  body:
    type: json
    data: |-
      {
        "profile": "default",
        "name": "默认系统设置"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
