info:
  name: 根据id下线账号
  type: http
  seq: 13
  tags:
    - account

http:
  method: POST
  url: "{{iam-service-url}}/account/offline/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
