info:
  name: 查询当前账号指定范围内的附近的账号
  type: http
  seq: 9
  tags:
    - account

http:
  method: GET
  url: "{{iam-service-url}}/account/nearby/1000"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
