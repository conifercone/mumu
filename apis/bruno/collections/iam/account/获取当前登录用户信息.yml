info:
  name: 获取当前登录用户信息
  type: http
  seq: 21
  tags:
    - account

http:
  method: GET
  url: "{{iam-service-url}}/account/currentLoginAccount"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
