info:
  name: 分页获取账号（不查询总数）
  type: http
  seq: 1
  tags:
    - account

http:
  method: GET
  url: "{{iam-service-url}}/account/findAllSlice?current=1&pageSize=10"
  params:
    - name: current
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
