info:
  name: 获取所有城市详细信息（不包含省（州）、市信息）
  type: http
  seq: 9

http:
  method: GET
  url: "{{genix-service-url}}/country/getCountries"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
