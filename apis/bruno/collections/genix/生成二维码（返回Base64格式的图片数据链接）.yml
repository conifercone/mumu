info:
  name: 生成二维码（返回Base64格式的图片数据链接）
  type: http
  seq: 6

http:
  method: GET
  url: "{{genix-service-url}}/qrCode/dataUrl?content=测试"
  params:
    - name: content
      value: 测试
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
