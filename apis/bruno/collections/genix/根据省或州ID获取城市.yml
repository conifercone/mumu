info:
  name: 根据省或州ID获取城市
  type: http
  seq: 11

http:
  method: GET
  url: "{{genix-service-url}}/country/getCitiesByStateId/2252"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
