info:
  name: 获取雪花算法ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{genix-service-url}}/pk/snowflake"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
