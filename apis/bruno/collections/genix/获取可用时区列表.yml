info:
  name: 获取可用时区列表
  type: http
  seq: 4

http:
  method: GET
  url: "{{genix-service-url}}/time/timezone/available"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
